{% extends 'mainvista/base.html' %}
{% block content %}

<div class="card">
    <div class="card-header">
        <div>
            <h1>📋 Panel de Inventario Retail</h1>
            <div class="subtitle">Conectado a db.sqlite3 a través del Patrón DAO (Fase 3 - CRUD Completo).</div>
        </div>
        <a href="?pestaana=registro&usuario={{ usuario_activo }}" class="btn">➕ Registrar Producto</a>
    </div>

    {% if alertas %}
    <div class="alert">
        <span style="font-size: 18px;">⚠️</span>
        <div>
            <h3 class="alert-title">ATENCIÓN: Productos en Niveles Críticos</h3>
            <ul class="alert-desc" style="list-style-type: disc; margin-left: 16px;">
                {% for alerta in alertas %}
                    <li>{{ alerta.nombre }} (SKU: {{ alerta.sku }}) - Quedan {{ alerta.stock_actual }} pzas.</li>
                {% endfor %}
            </ul>
        </div>
    </div>
    {% endif %}

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>SKU</th>
                    <th>Producto</th>
                    <th>Categoría</th>
                    <th>Precio</th>
                    <th>Existencias</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for p in productos %}
                <tr>
                    <td class="sku">{{ p.sku }}</td>
                    <td style="font-weight: 500;">{{ p.nombre }}</td>
                    <td>{{ p.categoria.nombre }}</td>
                    <td style="font-weight: 500; color: #374151;">${{ p.precio }}</td>
                    <td style="font-weight: 600;">{{ p.stock_actual }} pzas</td>
                    <td>
                        {% if p.stock_actual <= p.stock_minimo %}
                            <span class="badge-critico">🔴 Crítico</span>
                        {% else %}
                            <span class="badge-optimo">🟢 Óptimo</span>
                        {% endif %}
                    </td>
                    <td style="display: flex; gap: 8px;">
                        <!-- BOTÓN DE EDITAR (Soporta requerimiento de Actualización de registros) -->
                        <a href="?pestaana=editar&sku_editar={{ p.sku }}&usuario={{ usuario_activo }}" class="btn" style="padding: 6px 12px; font-size: 12px; background-color: #f59e0b;">✏️ Editar</a>
                        
                        <form method="POST" action="?pestaana=tablero&usuario={{ usuario_activo }}" style="margin:0;" onsubmit="return confirm('¿Seguro que deseas eliminar este producto?');">
                            {% csrf_token %}
                            <input type="hidden" name="accion" value="eliminar_producto">
                            <input type="hidden" name="sku_a_borrar" value="{{ p.sku }}">
                            <button type="submit" class="btn-danger">🗑️ Eliminar</button>
                        </form>
                    </td>
                </tr>
                {% empty %}
                <tr><td colspan="7" style="text-align: center; color: #9ca3af; padding: 20px;">No hay productos registrados. Usa el botón superior.</td></tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

{% endblock %}
